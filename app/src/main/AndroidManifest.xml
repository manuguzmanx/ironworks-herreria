<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginprueba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".Acerca"
            android:label="@string/tituloAcerca"></activity>
        <activity android:name=".QuejasSugerencias"
            android:label="@string/tituloQuejasYSugerencias"/>
        <activity android:name=".Contactanos"
            android:label="@string/tituloContactanos"/>
        <activity android:name=".Cotizar"
            android:label="@string/tituloCotizar"/>
        <activity android:name=".Catalogo"
            android:label="@string/tituloCatalogo"/>
        <activity android:name=".HomeActivity"
            android:label="@string/tituloMenu"/>
        <activity android:name=".MainActivity"
            android:label="@string/tituloIronworks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>